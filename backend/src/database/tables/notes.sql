CREATE TABLE Notes (
        id VARCHAR(100) NOT NULL ,
        title VARCHAR(200) NOT NULL,
        content VARCHAR(200) NOT NULL,
        createdAt VARCHAR(100)
)

DROP TABLE Notes

SELECT * FROM Notes
